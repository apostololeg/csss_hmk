require.config({
    paths: {
        core: "core",
        jquery:  "lib/jquery",
        modernizr: "lib/modernizr"
        // tap: "lib/tap",
        // ua: "lib/ua",
        // slider: "lib/slider"
    }
});

require(['index']);
